import 'package:flutter/cupertino.dart';

class SizedBoxfixo extends StatelessWidget {
  const SizedBoxfixo({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 20,
    );
  }
}
